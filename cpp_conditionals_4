#include <iostream>

using namespace std;

int main()
{
    int val;
    
    cout << "Enter your age: ";
    cin >> val;
    
    if ((val >= 18) ? cout << "More or equal to 18" : cout << "Less than 18")
    
    return 0;
}
